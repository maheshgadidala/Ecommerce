package com.JavaEcommerce.Ecommerce.repo.UserRepostory;

// This file originally contained a duplicate UserRepository interface (typo package name).
// It has been replaced with a harmless marker class so Spring Data will not auto-register
// a duplicate `userRepository` bean. The real repository lives at
// `com.JavaEcommerce.Ecommerce.repo.UserRepository`.

class UserRepositoryMarker {
    // marker class - intentionally empty
}
